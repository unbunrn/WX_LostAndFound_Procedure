<!-- 寻主 寻物 -->
<view class="tab">
  <view class="tab_item {{ select!== index ?'tab-default' : ''}}" wx:for="{{ tabList }}" wx:key="index" bindtap='selectTab' data-id="{{index}}">
    <text>{{item}}</text>
    <view wx:if="{{select==index}}" class="tab_line"></view>
  </view>
</view>